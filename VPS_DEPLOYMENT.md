# 🚀 Полное руководство по развёртыванию SavannahMonarchy на Hostinger VPS

> **Для кого этот гайд:** Для полных новичков, которые никогда не работали с VPS.
> **Время выполнения:** 1-2 часа (включая ожидание DNS)
> **Результат:** Ваш сайт savannahmonarchy.com будет работать на вашем VPS

---

## 📑 Содержание

1. [Часть 0: Подготовка (что нужно сделать ДО начала)](#часть-0-подготовка)
2. [Часть 1: Настройка DNS](#часть-1-настройка-dns)
3. [Часть 2: Подключение к VPS через SSH](#часть-2-подключение-к-vps)
4. [Часть 3: Первоначальная настройка сервера](#часть-3-настройка-сервера)
5. [Часть 4: Установка программ](#часть-4-установка-программ)
6. [Часть 5: Клонирование и сборка проекта](#часть-5-сборка-проекта)
7. [Часть 6: Настройка Nginx](#часть-6-настройка-nginx)
8. [Часть 7: SSL-сертификат (HTTPS)](#часть-7-ssl-сертификат)
9. [Часть 8: Тестирование сайта](#часть-8-тестирование)
10. [Часть 9: Обновление сайта](#часть-9-обновление)
11. [Часть 10: Решение проблем](#часть-10-проблемы)

---

# Часть 0: Подготовка

> ⏱️ **Время:** 15-20 минут

Перед тем как начать работу с VPS, нужно подготовить несколько вещей.

---

## 0.1 Требования

Убедитесь, что у вас есть:

| Что нужно | Где взять |
|-----------|-----------|
| ✅ VPS на Hostinger | Должен быть уже куплен |
| ✅ Домен savannahmonarchy.com | Должен быть уже куплен |
| ✅ GitHub аккаунт | https://github.com |
| ✅ Проект подключен к GitHub | Настраивается в Lovable |

---

## 0.2 Подключение Lovable к GitHub

> 💡 **Зачем это нужно:** Чтобы код вашего сайта был доступен на GitHub, откуда VPS сможет его скачать.

### Шаг 1: Откройте Lovable

1. Перейдите на https://lovable.dev
2. Войдите в свой аккаунт
3. Откройте проект SavannahMonarchy

### Шаг 2: Найдите кнопку GitHub

1. В верхней части экрана найдите кнопку **"GitHub"** (справа от названия проекта)
2. Нажмите на неё

### Шаг 3: Подключите GitHub

1. Нажмите **"Connect to GitHub"**
2. Откроется страница авторизации GitHub
3. Нажмите **"Authorize Lovable"** (зелёная кнопка)
4. Выберите свой аккаунт GitHub

### Шаг 4: Создайте репозиторий

1. После авторизации вернётесь в Lovable
2. Нажмите **"Create Repository"**
3. Введите название: `savannahmonarchy`
4. Нажмите **"Create"**

### Шаг 5: Запишите URL репозитория

После создания репозитория вы увидите ссылку вида:
```
https://github.com/ВАШ_ЛОГИН/savannahmonarchy
```

📋 **Запишите эту ссылку!** Она понадобится позже.

> ✅ **Проверка:** Перейдите по ссылке репозитория в браузере. Вы должны видеть файлы вашего проекта.

---

## 0.3 Где найти IP-адрес VPS

> 💡 **Что такое IP-адрес:** Это уникальный адрес вашего сервера в интернете, как почтовый адрес дома.

### Шаг 1: Войдите в Hostinger

1. Откройте https://www.hostinger.com
2. Нажмите **"Войти"** в правом верхнем углу
3. Введите email и пароль

### Шаг 2: Найдите раздел VPS

1. После входа вы попадёте в панель управления (hPanel)
2. В левом меню найдите **"VPS"** или **"Хостинг" → "VPS"**
3. Нажмите на него

### Шаг 3: Выберите ваш VPS

1. Вы увидите список ваших VPS серверов
2. Нажмите на ваш VPS (обычно он один)

### Шаг 4: Найдите IP-адрес

1. На странице информации о VPS найдите блок **"SSH доступ"** или **"Информация о сервере"**
2. IP-адрес будет выглядеть примерно так: `123.456.78.90`

📋 **Запишите этот IP-адрес!**

> 💡 **Пример:** Если ваш IP `185.199.110.153`, то именно его вы будете использовать везде, где написано "ВАШ_IP".

---

## 0.4 Где найти логин и пароль от VPS

На той же странице в Hostinger:

1. **Логин** обычно: `root`
2. **Пароль** находится в разделе **"SSH доступ"** или **"Root пароль"**
   - Если пароль скрыт, нажмите на иконку глаза 👁️ чтобы его показать
   - Если пароля нет, нажмите **"Сбросить пароль"**

📋 **Запишите логин и пароль!**

---

## 0.5 Какая ОС должна быть на VPS

> ⚠️ **ВАЖНО:** Этот гайд написан для Ubuntu 22.04 или 24.04

### Как проверить/изменить ОС:

1. В панели управления VPS найдите раздел **"Операционная система"** или **"OS"**
2. Должно быть написано **Ubuntu 22.04** или **Ubuntu 24.04**
3. Если другая ОС — нажмите **"Переустановить ОС"** и выберите **Ubuntu 22.04 LTS**

> ⚠️ **Переустановка ОС удалит все данные на сервере!** Делайте только если VPS новый.

---

## 0.6 Чеклист подготовки

Перед переходом к следующей части убедитесь:

- [ ] GitHub подключен к Lovable
- [ ] Репозиторий создан и URL записан
- [ ] IP-адрес VPS записан
- [ ] Логин и пароль от VPS записаны
- [ ] На VPS установлена Ubuntu 22.04 или 24.04

---

# Часть 1: Настройка DNS

> ⏱️ **Время:** 10 минут настройки + до 24 часов ожидания
> 
> ⚠️ **ВАЖНО:** Начните с этого шага! DNS нужно время чтобы "распространиться" по интернету.

---

## 1.1 Что такое DNS (простыми словами)

DNS — это как телефонная книга интернета:
- Когда вы вводите `savannahmonarchy.com`, компьютер не понимает что это
- DNS переводит название в IP-адрес (например, `185.199.110.153`)
- Теперь компьютер знает, куда отправить запрос

**Наша задача:** Сказать DNS, что `savannahmonarchy.com` = IP вашего VPS.

---

## 1.2 Как зайти в настройки DNS

### Шаг 1: Войдите в Hostinger

1. Откройте https://www.hostinger.com
2. Войдите в аккаунт

### Шаг 2: Найдите раздел "Домены"

1. В левом меню нажмите **"Домены"**
2. Найдите домен `savannahmonarchy.com` в списке
3. Нажмите на него

### Шаг 3: Откройте DNS-зону

1. На странице домена найдите раздел **"DNS / Nameservers"** или **"DNS-зона"**
2. Нажмите **"Редактировать DNS-зону"** или **"Manage DNS"**

---

## 1.3 Какие DNS-записи создать

> 💡 **Мы создадим 2 записи:** одну для `savannahmonarchy.com`, другую для `www.savannahmonarchy.com`

### Запись 1: Основной домен (A-запись)

| Поле | Что вводить |
|------|-------------|
| **Тип** | A |
| **Имя** | `@` (или оставить пустым) |
| **Указывает на** | `ВАШ_IP_АДРЕС_VPS` (например: 185.199.110.153) |
| **TTL** | 14400 (или "По умолчанию") |

### Запись 2: WWW-поддомен (A-запись)

| Поле | Что вводить |
|------|-------------|
| **Тип** | A |
| **Имя** | `www` |
| **Указывает на** | `ВАШ_IP_АДРЕС_VPS` (тот же IP) |
| **TTL** | 14400 |

---

## 1.4 Пошаговое создание A-записи

### Шаг 1: Удалите старые записи (если есть)

1. Посмотрите список существующих записей
2. Если есть старые A-записи для `@` или `www` — удалите их (кнопка 🗑️)

### Шаг 2: Создайте новую запись

1. Найдите кнопку **"Добавить запись"** или **"+ Add Record"**
2. Нажмите на неё

### Шаг 3: Заполните поля

1. **Тип:** Выберите `A` из выпадающего списка
2. **Имя (Name/Host):** Введите `@`
3. **Указывает на (Points to/Value):** Введите ваш IP-адрес VPS
4. **TTL:** Оставьте как есть (или выберите 14400)

### Шаг 4: Сохраните

1. Нажмите **"Сохранить"** или **"Add Record"**

### Шаг 5: Повторите для WWW

Создайте ещё одну запись по тому же принципу, но:
- **Имя:** введите `www` вместо `@`
- Остальное — то же самое

---

## 1.5 Как проверить что DNS настроен

> ⏱️ **DNS обновляется от 5 минут до 24 часов.** Обычно 15-30 минут.

### Способ 1: Онлайн-сервис

1. Откройте https://dnschecker.org
2. Введите `savannahmonarchy.com`
3. Выберите тип: `A`
4. Нажмите **"Search"**
5. Если видите ваш IP-адрес с зелёными галочками — DNS работает ✅

### Способ 2: Командная строка (Windows)

1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. Введите команду:
```
nslookup savannahmonarchy.com
```
4. Если видите ваш IP — DNS работает ✅

### Способ 3: Терминал (Mac)

1. Откройте Терминал (Finder → Программы → Утилиты → Терминал)
2. Введите:
```
dig savannahmonarchy.com
```
3. В секции "ANSWER" должен быть ваш IP ✅

---

## 1.6 Что делать пока ждёте DNS

DNS обновляется в фоне, вы можете продолжать настройку VPS!

Переходите к **Части 2** — подключение к серверу.

---

## 1.7 Чеклист DNS

- [ ] A-запись для `@` создана с вашим IP
- [ ] A-запись для `www` создана с вашим IP
- [ ] Проверка через dnschecker.org показывает ваш IP (может появиться позже)

---

# Часть 2: Подключение к VPS

> ⏱️ **Время:** 5-10 минут

---

## 2.1 Что такое SSH (простыми словами)

**SSH** — это способ удалённо управлять сервером через текстовые команды.

Представьте:
- Ваш VPS — это компьютер без монитора, который стоит где-то далеко
- SSH позволяет "подключиться" к нему и вводить команды
- Как будто вы сидите прямо за этим компьютером

---

## 2.2 Выберите вашу операционную систему

| Ваш компьютер | Используйте |
|---------------|-------------|
| **Windows 10/11** | Встроенный терминал или PuTTY |
| **Mac** | Встроенный Терминал |
| **Linux** | Встроенный Терминал |

---

## 2.3 Подключение с Windows 10/11 (простой способ)

> 💡 В Windows 10/11 есть встроенный SSH, дополнительные программы не нужны!

### Шаг 1: Откройте PowerShell

1. Нажмите кнопку **Пуск** (Windows)
2. Начните печатать: `PowerShell`
3. Нажмите на **"Windows PowerShell"**

### Шаг 2: Введите команду подключения

Введите (замените `ВАШ_IP` на реальный IP вашего VPS):

```bash
ssh root@ВАШ_IP
```

**Пример с реальным IP:**
```bash
ssh root@185.199.110.153
```

### Шаг 3: Подтвердите подключение

При первом подключении появится сообщение:
```
The authenticity of host '185.199.110.153' can't be established.
ECDSA key fingerprint is SHA256:xxxxx...
Are you sure you want to continue connecting (yes/no/[fingerprint])?
```

**Что делать:** Напечатайте `yes` и нажмите Enter.

### Шаг 4: Введите пароль

1. Появится запрос: `root@185.199.110.153's password:`
2. Введите пароль от VPS (тот, что записали ранее)
3. ⚠️ **ВАЖНО:** При вводе пароля символы НЕ ОТОБРАЖАЮТСЯ — это нормально!
4. Нажмите Enter

### Шаг 5: Проверьте успешное подключение

Если увидели что-то вроде:
```
Welcome to Ubuntu 22.04.3 LTS
root@vps-xxxxx:~#
```
**Поздравляю! Вы подключились к серверу!** 🎉

---

## 2.4 Подключение с Windows через PuTTY (альтернативный способ)

> Используйте этот способ, если простой не работает.

### Шаг 1: Скачайте PuTTY

1. Откройте https://www.putty.org
2. Нажмите **"Download PuTTY"**
3. Скачайте **"putty-64bit-X.XX-installer.msi"**
4. Запустите установщик и нажимайте **"Next"** → **"Install"** → **"Finish"**

### Шаг 2: Запустите PuTTY

1. Нажмите **Пуск**
2. Найдите и откройте **"PuTTY"**

### Шаг 3: Настройте подключение

В окне PuTTY:

| Поле | Что вводить |
|------|-------------|
| **Host Name (or IP address)** | Ваш IP-адрес VPS |
| **Port** | 22 |
| **Connection type** | SSH (уже выбрано) |

### Шаг 4: Подключитесь

1. Нажмите **"Open"**
2. Если появится окно "PuTTY Security Alert" — нажмите **"Accept"**
3. Откроется чёрное окно терминала

### Шаг 5: Войдите

1. `login as:` — введите `root` и нажмите Enter
2. `root@xxx's password:` — введите пароль и нажмите Enter

---

## 2.5 Подключение с Mac

### Шаг 1: Откройте Терминал

**Способ 1 (быстрый):**
1. Нажмите `Cmd + Пробел`
2. Введите `Terminal`
3. Нажмите Enter

**Способ 2:**
1. Откройте Finder
2. Перейдите: Программы → Утилиты → Терминал

### Шаг 2: Введите команду

```bash
ssh root@ВАШ_IP
```

**Пример:**
```bash
ssh root@185.199.110.153
```

### Шаг 3: Подтвердите и введите пароль

1. На вопрос про fingerprint напечатайте `yes`
2. Введите пароль (символы не видны — это нормально)

---

## 2.6 Частые проблемы при подключении

### Проблема: "Connection refused"

**Причины и решения:**
1. **VPS выключен** → Включите VPS в панели Hostinger
2. **Неправильный IP** → Проверьте IP в панели Hostinger
3. **Firewall блокирует** → Обратитесь в поддержку Hostinger

### Проблема: "Permission denied"

**Причина:** Неправильный пароль

**Решение:**
1. Проверьте, что вводите правильный пароль
2. Сбросьте пароль в панели Hostinger (раздел "Root пароль" → "Сбросить")

### Проблема: "Connection timed out"

**Причины:**
1. Неправильный IP-адрес
2. VPS ещё не запустился (подождите 2-3 минуты после включения)

---

## 2.7 Чеклист подключения

- [ ] Подключились к VPS через SSH
- [ ] Видите приглашение командной строки: `root@vps-xxxxx:~#`

---

# Часть 3: Настройка сервера

> ⏱️ **Время:** 10-15 минут
>
> ⚠️ **Все команды выполняются в терминале, подключенном к VPS!**

---

## 3.1 Обновление системы

> 💡 **Зачем:** Устанавливаем последние обновления безопасности.

### Шаг 1: Обновите список пакетов

Скопируйте и вставьте эту команду, затем нажмите Enter:

```bash
apt update
```

**Что происходит:** Система проверяет, какие обновления доступны.

### Шаг 2: Установите обновления

```bash
apt upgrade -y
```

**Что происходит:** Устанавливаются все доступные обновления.

> ⏱️ Это может занять 2-5 минут. Просто ждите.

---

## 3.2 Настройка Firewall (файервола)

> 💡 **Зачем:** Firewall блокирует нежелательные подключения к серверу.

### Шаг 1: Разрешите SSH (ВАЖНО!)

```bash
sudo ufw allow ssh
```

> ⚠️ **КРИТИЧЕСКИ ВАЖНО:** Если не разрешить SSH перед включением firewall, вы потеряете доступ к серверу!

### Шаг 2: Разрешите HTTP (порт 80)

```bash
sudo ufw allow 80
```

### Шаг 3: Разрешите HTTPS (порт 443)

```bash
sudo ufw allow 443
```

### Шаг 4: Включите Firewall

```bash
sudo ufw enable
```

Появится вопрос — введите `y` и нажмите Enter.

### Шаг 5: Проверьте настройки

```bash
sudo ufw status
```

Вы должны увидеть:
```
Status: active

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
```

✅ Firewall настроен!

---

## 3.3 Чеклист настройки сервера

- [ ] Система обновлена
- [ ] Firewall настроен и включен
- [ ] Разрешены порты: 22 (SSH), 80 (HTTP), 443 (HTTPS)

---

# Часть 4: Установка программ

> ⏱️ **Время:** 10-15 минут

---

## 4.1 Что мы будем устанавливать

| Программа | Зачем нужна |
|-----------|-------------|
| **Node.js** | Для сборки проекта |
| **Nginx** | Веб-сервер для вашего сайта |
| **Git** | Для скачивания проекта с GitHub |

---

## 4.2 Установка Node.js 20

### Шаг 1: Добавьте репозиторий Node.js

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
```

### Шаг 2: Установите Node.js

```bash
sudo apt install -y nodejs
```

### Шаг 3: Проверьте установку

```bash
node --version
```

**Должно показать:** `v20.x.x`

```bash
npm --version
```

**Должно показать:** `10.x.x`

---

## 4.3 Установка Nginx

### Шаг 1: Установите Nginx

```bash
sudo apt install -y nginx
```

### Шаг 2: Запустите Nginx

```bash
sudo systemctl start nginx
sudo systemctl enable nginx
```

### Шаг 3: Проверьте в браузере

1. Откройте браузер
2. Введите IP-адрес вашего VPS: `http://ВАШ_IP`
3. Вы должны увидеть страницу **"Welcome to nginx!"**

---

## 4.4 Установка Git

```bash
sudo apt install -y git
```

Проверка:
```bash
git --version
```

---

## 4.5 Чеклист установки программ

- [ ] Node.js установлен (`node --version` показывает v20.x.x)
- [ ] Nginx установлен и запущен
- [ ] В браузере по IP открывается "Welcome to nginx!"
- [ ] Git установлен

---

# Часть 5: Сборка проекта

> ⏱️ **Время:** 10-20 минут

---

## 5.1 Клонирование проекта с GitHub

### Шаг 1: Перейдите в домашнюю папку

```bash
cd ~
```

### Шаг 2: Клонируйте репозиторий

Замените `ВАШ_GITHUB_ЛОГИН` на ваш логин GitHub:

```bash
git clone https://github.com/ВАШ_GITHUB_ЛОГИН/savannahmonarchy.git
```

**Пример:**
```bash
git clone https://github.com/johnsmith/savannahmonarchy.git
```

### Шаг 3: Перейдите в папку проекта

```bash
cd savannahmonarchy
```

---

## 5.2 Создание файла .env

### Шаг 1: Создайте файл

```bash
nano .env
```

### Шаг 2: Вставьте содержимое

Скопируйте этот текст и вставьте в терминал:

```env
VITE_SUPABASE_URL=https://rujvbcxpnzpikkmgdkfs.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJ1anZiY3hwbnpwaWtrbWdka2ZzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjAwMTc2NzUsImV4cCI6MjA3NTU5MzY3NX0.hD3auiGF0hLclhggC_43AvtKS1TxhQTeekBoHO9sxmM
VITE_SUPABASE_PROJECT_ID=rujvbcxpnzpikkmgdkfs
```

### Шаг 3: Сохраните файл

1. Нажмите `Ctrl + X` (выход)
2. Нажмите `Y` (да, сохранить)
3. Нажмите `Enter`

---

## 5.3 Установка зависимостей

```bash
npm install
```

> ⏱️ Это может занять 2-5 минут.

---

## 5.4 Сборка проекта

```bash
npm run build
```

> ⏱️ Это может занять 1-3 минуты.

**Успешный результат:**
```
✓ built in Xs
```

---

## 5.5 Копирование сайта в папку Nginx

```bash
sudo mkdir -p /var/www/savannahmonarchy.com
sudo cp -r dist/* /var/www/savannahmonarchy.com/
sudo chown -R www-data:www-data /var/www/savannahmonarchy.com
```

---

## 5.6 Если GitHub просит авторизацию

1. Откройте https://github.com/settings/tokens
2. Нажмите **"Generate new token (classic)"**
3. Название: `VPS Access`
4. Отметьте галочку **"repo"**
5. Нажмите **"Generate token"**
6. Скопируйте токен

При клонировании вместо пароля вводите этот токен.

---

## 5.7 Чеклист сборки

- [ ] Проект клонирован
- [ ] Файл .env создан
- [ ] `npm install` выполнен
- [ ] `npm run build` выполнен
- [ ] Файлы скопированы в /var/www/savannahmonarchy.com/

---

# Часть 6: Настройка Nginx

> ⏱️ **Время:** 10-15 минут

---

## 6.1 Создание конфигурационного файла

```bash
sudo nano /etc/nginx/sites-available/savannahmonarchy.com
```

---

## 6.2 Вставьте конфигурацию

Скопируйте весь этот блок и вставьте:

```nginx
server {
    listen 80;
    listen [::]:80;
    
    server_name savannahmonarchy.com www.savannahmonarchy.com;
    
    root /var/www/savannahmonarchy.com;
    index index.html;
    
    # Gzip сжатие для ускорения загрузки
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml application/javascript;
    
    # Кэширование статических файлов
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # Основная обработка запросов (для SPA)
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Безопасность
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}
```

Сохраните: `Ctrl + X` → `Y` → `Enter`

---

## 6.3 Активация конфигурации

```bash
sudo ln -s /etc/nginx/sites-available/savannahmonarchy.com /etc/nginx/sites-enabled/
sudo rm /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

**Успешный результат `nginx -t`:**
```
nginx: configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

---

## 6.4 Чеклист Nginx

- [ ] Конфигурационный файл создан
- [ ] `nginx -t` показывает "syntax is ok"
- [ ] Nginx перезагружен
- [ ] Сайт открывается по IP-адресу

---

# Часть 7: SSL-сертификат (HTTPS)

> ⚠️ **ВАЖНО:** SSL можно установить ТОЛЬКО после того, как DNS заработал!
> Проверьте: `http://savannahmonarchy.com` должен открываться.

---

## 7.1 Установка Certbot

```bash
sudo apt install -y certbot python3-certbot-nginx
```

---

## 7.2 Получение SSL-сертификата

```bash
sudo certbot --nginx -d savannahmonarchy.com -d www.savannahmonarchy.com
```

### Ответьте на вопросы:

1. **Email:** Введите ваш email и нажмите Enter
2. **Согласие с условиями:** Введите `Y` и нажмите Enter
3. **Рассылка:** Введите `N` и нажмите Enter

**Успешный результат:**
```
Congratulations! You have successfully enabled HTTPS on https://savannahmonarchy.com
```

---

## 7.3 Проверка HTTPS

1. В браузере откройте: `https://savannahmonarchy.com`
2. Вы должны увидеть замочек 🔒 слева от адреса

---

## 7.4 Проверка автообновления

```bash
sudo certbot renew --dry-run
```

**Успешный результат:**
```
Congratulations, all renewals succeeded.
```

---

## 7.5 Чеклист SSL

- [ ] Certbot установлен
- [ ] Сертификат получен
- [ ] `https://savannahmonarchy.com` открывается с замочком 🔒
- [ ] Автообновление работает

---

# Часть 8: Тестирование сайта

---

## 8.1 Базовая проверка

1. Откройте `https://savannahmonarchy.com`
2. Проверьте все страницы
3. Проверьте мобильную версию (F12 → иконка телефона)

---

## 8.2 Проверка подключения к базе данных

1. Откройте DevTools (F12)
2. Вкладка **"Network"**
3. Обновите страницу
4. Найдите запросы к `supabase.co`
5. Статус должен быть 200 ✅

---

## 8.3 Проверка консоли

1. DevTools (F12) → **"Console"**
2. Красных ошибок быть не должно

---

# Часть 9: Обновление сайта

После изменений в Lovable выполните эти команды:

```bash
cd ~/savannahmonarchy
git pull
npm install
npm run build
sudo cp -r dist/* /var/www/savannahmonarchy.com/
sudo chown -R www-data:www-data /var/www/savannahmonarchy.com
```

---

## 9.1 Скрипт автоматического обновления

Создайте скрипт:

```bash
nano ~/update-site.sh
```

Вставьте:

```bash
#!/bin/bash

echo "🚀 Начинаю обновление SavannahMonarchy..."

cd ~/savannahmonarchy

echo "📥 Получаю последние изменения из GitHub..."
git pull

echo "📦 Устанавливаю зависимости..."
npm install

echo "🔨 Собираю проект..."
npm run build

echo "📂 Копирую файлы..."
sudo cp -r dist/* /var/www/savannahmonarchy.com/
sudo chown -R www-data:www-data /var/www/savannahmonarchy.com

echo "✅ Обновление завершено!"
echo "🌐 Сайт: https://savannahmonarchy.com"
```

Сохраните и сделайте исполняемым:

```bash
chmod +x ~/update-site.sh
```

Теперь для обновления:

```bash
~/update-site.sh
```

---

# Часть 10: Решение проблем

---

## 10.1 Сайт не открывается

```bash
# Проверьте Nginx
sudo systemctl status nginx

# Если не работает, запустите
sudo systemctl start nginx
```

---

## 10.2 Ошибка 502 Bad Gateway

```bash
# Проверьте файлы
ls /var/www/savannahmonarchy.com/

# Должен быть index.html
```

---

## 10.3 Ошибка 403 Forbidden

```bash
sudo chown -R www-data:www-data /var/www/savannahmonarchy.com
sudo chmod -R 755 /var/www/savannahmonarchy.com
```

---

## 10.4 npm падает (не хватает памяти)

Создайте swap-файл:

```bash
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

После этого повторите `npm install` и `npm run build`.

---

## 10.5 SSL не работает

```bash
# DNS ещё не обновился — подождите
# Проверьте на https://dnschecker.org
```

---

## 10.6 Просмотр логов

```bash
# Логи ошибок Nginx
sudo tail -50 /var/log/nginx/error.log

# Логи в реальном времени
sudo tail -f /var/log/nginx/error.log
```

---

## 10.7 Полезные команды

| Команда | Описание |
|---------|----------|
| `sudo systemctl restart nginx` | Перезапустить Nginx |
| `sudo systemctl status nginx` | Проверить статус Nginx |
| `sudo nginx -t` | Проверить конфигурацию |
| `df -h` | Свободное место на диске |
| `free -m` | Оперативная память |

---

# 🎉 Готово!

Ваш сайт **SavannahMonarchy** успешно развёрнут на VPS!

## Что у вас теперь есть:

- ✅ Сайт работает на вашем VPS
- ✅ Домен savannahmonarchy.com настроен
- ✅ SSL-сертификат установлен (HTTPS)
- ✅ База данных работает через Lovable Cloud
- ✅ Скрипт для быстрого обновления

## Что делать дальше:

1. **Регулярно обновляйте** сайт после изменений в Lovable: `~/update-site.sh`
2. **Обновляйте систему** раз в месяц: `sudo apt update && sudo apt upgrade -y`

---

**Удачи с проектом SavannahMonarchy! 🐆**
